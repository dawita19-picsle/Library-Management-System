# Smart Library Management System — Database Configuration
# Credentials loaded at runtime — not hardcoded in source code

db.host=localhost
db.port=1433
db.name=LibraryDB
db.user=sa
db.password=Adisu@123
db.encrypt=true
db.trustServerCertificate=true
